-- Programming in Haskell

-- write an expression that checks whether a list is empty.
let isempty x = if null(x) then True else False

-- write an expression that checks whether a list has one element.
let hasone x = if length(x) == 1 then True else False
